---
id: icon-button
title: IconButton
sidebar_position: 30
---

import ComponentPreview from '@site/src/components/ComponentPreview';
import PreviewPair from '@site/src/components/PreviewPair';
import { IconButton, Row } from 'vajra-ui';
import { BellIcon } from 'vajra-ui-icons';

# IconButton

A pressable, icon-only button with six visual variants and five sizes, mirroring Button's token-driven styling. Vajra UI ships no bundled icons — `icon` accepts any component matching `TVajraIconComponent`. The examples below use [`vajra-ui-icons`](https://github.com/devraj-labs/vajra-ui-icons), a small set of Lucide icons pre-adapted to that contract.

## Variants

<PreviewPair>
<ComponentPreview>
  <Row gap="s-2" wrap="wrap">
    <IconButton variant="solid" icon={BellIcon} onPress={() => {}} />
    <IconButton variant="subtle" icon={BellIcon} onPress={() => {}} />
    <IconButton variant="surface" icon={BellIcon} onPress={() => {}} />
    <IconButton variant="outline" icon={BellIcon} onPress={() => {}} />
    <IconButton variant="ghost" icon={BellIcon} onPress={() => {}} />
    <IconButton variant="plain" icon={BellIcon} onPress={() => {}} />
  </Row>
</ComponentPreview>

```tsx
import { BellIcon } from 'vajra-ui-icons';

<IconButton variant="solid" icon={BellIcon} onPress={() => {}} />
<IconButton variant="outline" icon={BellIcon} onPress={() => {}} />
```
</PreviewPair>

## Sizes

<PreviewPair>
<ComponentPreview>
  <Row gap="s-2" align="center" wrap="wrap">
    <IconButton size="xs" icon={BellIcon} onPress={() => {}} />
    <IconButton size="sm" icon={BellIcon} onPress={() => {}} />
    <IconButton size="md" icon={BellIcon} onPress={() => {}} />
    <IconButton size="lg" icon={BellIcon} onPress={() => {}} />
    <IconButton size="xl" icon={BellIcon} onPress={() => {}} />
  </Row>
</ComponentPreview>

## Loading & disabled

<ComponentPreview>
  <Row gap="s-2" wrap="wrap">
    <IconButton icon={BellIcon} isLoading onPress={() => {}} />
    <IconButton icon={BellIcon} isDisabled onPress={() => {}} />
  </Row>
</ComponentPreview>

```tsx
<IconButton icon={BellIcon} isLoading onPress={() => {}} />
<IconButton icon={BellIcon} isDisabled onPress={() => {}} />
```
</PreviewPair>

## Props

| Prop | Type | Default | Description |
|---|---|---|---|
| `icon` | `TVajraIconComponent` | — | Required. Icon component to render; `size` and `color` are injected automatically. |
| `iconColor` | `TVajraColors` | — | Overrides the color resolved from `variant`. |
| `variant` | `'solid' \| 'subtle' \| 'surface' \| 'outline' \| 'ghost' \| 'plain'` | `'solid'` | Visual style. |
| `size` | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl'` | `'md'` | Container and icon size. |
| `isDisabled` | `boolean` | `false` | Disables interaction and dims opacity. |
| `isLoading` | `boolean` | `false` | Shows a spinner in place of the icon. |
| `loading` | `{ loader?: ReactNode }` | — | Customize the loading spinner. |

Also accepts all `Pressable` props (`bg`, `borderColor`, spacing, etc.) except `disabled` — use `isDisabled` instead.
